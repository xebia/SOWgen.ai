# Backend API Configuration
VITE_API_URL=http://localhost:8000
VITE_USE_BACKEND=false

# Set to 'true' to use MongoDB backend instead of local storage
# When false, the app uses GitHub Spark KV store for local storage
